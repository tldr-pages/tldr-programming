# bin

> Convert an integer number to a binary string prefixed with "0b".
> See also: `format`.
> More information: <https://docs.python.org/3/library/functions.html#bin>.

- Convert an integer to a binary string:

```python
bin(3)
```
