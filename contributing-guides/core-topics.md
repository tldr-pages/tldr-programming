### Basics
- `hello-world`
- `variables` (declaration, scopes, constants)
- `loops` (for, while, iterators)
- `conditionals` (if, switch, match)
- `type-casting` (converting string to int, float to int, etc.)
### Data structures
- `arrays` | `lists` | `slices`
- `maps` | `dictionaries` | `hashmaps`
- `sets`
- `strings` (interpolation, splitting, joining, substrings)
- `tuples` | `records` (if supported)
- `sorting` (custom comparators, sorting by key)
### Functions & OOP
- `functions` (declaration, default args, variadic)
- `lambdas` | `anonymous-functions`
- `classes` | `structs` (constructors, methods)
- `interfaces` | `traits` | `protocols`
- `imports` | `modules` (exporting/importing code)
### Error handling
- `exceptions` | `error-handling` (try/catch, panic/recover, Result types)
### Standard library & I/O
- `file-io` (reading, writing, apending)
- `json` (parsing, stringifying)
- `http-requests` (GET, POST, headers)
- `datetime` (formatting, parsing, timezones)
- `regex` (matching, replacing, capturing groups
- `math` (rounding, random, absolute)
### Concurrency & Async
- `async-await` | `promises`
- `threads` | `goroutines`
- `channels` | `queues`
### System & Environment
- `env-vars` (reading, setting)
- `cli-args` (parsing cli arguments)
- `child-processes` (executing shell commands)
